spring:
  datasource:
    url: jdbc:h2:mem:~/test
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    show-sql: false
    properties:
      hibernate:
        jdbc.batch_size: 500
        order_inserts: true
  main:
    banner-mode: 'off'
logging:
  level:
    com.p6spy: info
