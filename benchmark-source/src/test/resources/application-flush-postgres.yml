spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=benchmark
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQL95Dialect

