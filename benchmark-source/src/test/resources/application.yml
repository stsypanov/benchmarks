spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        jdbc.batch_size: 500
        generate_statistics: true
    hibernate:
      ddl-auto: create-drop
  jmx:
    enabled: false
  main:
    banner-mode: 'off'

logging:
  level:
    com.p6spy: info