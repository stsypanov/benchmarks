spring:
  jpa:
    show-sql: false
    properties:
      hibernate:
        jdbc.batch_size: 500
    hibernate:
      ddl-auto: create-drop
      generate_statistics: true
  jmx:
    enabled: false
  main:
    banner-mode: 'off'

logging:
  level:
    com.p6spy: info